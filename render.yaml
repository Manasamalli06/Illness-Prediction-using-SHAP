services:
  - type: web
    name: illness-prediction-shap
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python backend/app/app.py"
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
